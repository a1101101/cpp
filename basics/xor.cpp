#include <iostream>
#include <bitset>
using namespace std;

int main(){
    int a,b;
    cout << "Enter integer a: ";
    cin >> a;
    cout << "\n a: " << 
    cout << "Enter integer b: ";
    cin >> b;

    cout << "a ^ b : " << (a ^ b);

    return 0;
}
